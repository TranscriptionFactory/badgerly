---
"carbide": patch
---

Typing `[[~` now opens the assistant-session suggester, so session links no longer require typing the `◈` glyph. The alias is only accepted while the suggester is open; accepting a suggestion still writes the canonical `[[◈ id|title]]` target.
