---
"carbide": patch
---

Tags inside inline code, indented code, and math blocks are no longer indexed, and the editor no longer treats purely numeric `#123` as a tag, so the tag pane and editor pills agree.
