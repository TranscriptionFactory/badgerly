---
"carbide": minor
---

Tags are now opt-in: an inline `#tag` is a dimmed candidate until promoted from its pill, the tag pane's Candidates section, or by accepting it from `#` autocomplete. Frontmatter tags are promoted automatically. Suggested links only consider promoted tags.
